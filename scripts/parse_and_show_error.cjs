const fs=require('fs');const parser=require('@babel/parser');const s=fs.readFileSync('resources/js/Pages/SuperAdmin/RestaurantSettings.jsx','utf8');try{parser.parse(s,{sourceType:'module',plugins:['jsx','classProperties','optionalChaining','nullishCoalescingOperator','objectRestSpread']});console.log('PARSE_OK');}catch(e){console.error('ERROR_MESSAGE:',e.message); if(e.loc) console.error('LOC',e.loc); if(e.codeFrame) console.error('CODEFRAME:\n', e.codeFrame); // print surrounding lines
 const lines=s.split('\n'); const L=e.loc?e.loc.line:0; for(let i=Math.max(1,L-6); i<=Math.min(lines.length, L+6); i++){ console.log((i)+':', lines[i-1]); } process.exit(1);}